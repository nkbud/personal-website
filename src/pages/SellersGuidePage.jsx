import React from 'react';
import { Navigate } from 'react-router-dom';

const SellersGuidePage = () => {
  // This page is no longer used. Content is now part of sections.
  return <Navigate to="/" replace />;
};

export default SellersGuidePage;